# sqlviews
I created an employees table and inserted sample data. I used CREATE VIEW to define a view that displays employees with salary greater than 50000. The view stores the query logic and can be used like a table to retrieve filtered data, demonstrating abstraction and reusable SQL logic.
